name: Lighthouse CI Auto Report

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node (Latest LTS)
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      # package-lock.json が無い場合は CI が自動生成する
      - name: Generate lockfile if missing
        run: |
          if [ ! -f package-lock.json ]; then
            echo "package-lock.json が無いので自動生成します"
            npm init -y
            npm i -D @lhci/cli
          fi

      # npm ci は lockfile があると最速で依存関係を復元する
      - name: Install dependencies (cached)
        run: npm ci

      - name: Run Lighthouse (fast)
        run: |
          npx lhci autorun \
            --collect.staticDistDir=. \
            --collect.numberOfRuns=1 \
            --collect.settings.chromeFlags="--headless --no-sandbox --disable-dev-shm-usage"

      - name: Upload Report
        uses: actions/upload-artifact@v7
        with:
          name: lighthouse-report
          path: .lighthouseci/*
          include-hidden-files: true
